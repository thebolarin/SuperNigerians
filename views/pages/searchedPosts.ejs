<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../templates/head') %>
  <link rel="stylesheet" href="/css/posts.css" />
  <link rel="stylesheet" href="/css/animate.css"/>
</head>

<body style="background: #f8f9fa;">
  <%- include('../templates/sidebar') %>

  <main id="main">
    <div class="container animate__animated animate__slideInLeft">
      <div class="row" style="margin-right: 0px; margin-left: 0px;">
        <% if (posts.length === 0) { %>
            <div class="my-post col px-1 py-3 position-relative my-2">No posts match the search title parameter.</div>
        <% } %>
        <% if( posts &&  posts.length > 0){ %>
        <% for(var i=0; i <  posts.length; i++) { %>
        <div class="col-12 article d-flex flex-column flex-md-row mb-5">
          <div
            class="d-none d-md-flex justify-content-center profile-picture d-flex align-items-center mr-4 mb-2 mb-md-0">
            <img src=<%- posts[i].postPicture %> alt="user-profile-picture" class="img-fluid" />
          </div>
          <div class="article-details d-flex flex-column justify-content-center">
            <div class="text-caption">
              <a href=<%- "/" + posts[i].slug %>>
                <h3 class="caption text-center text-md-left"><%- posts[i].title %></h3>
              </a>
            </div>
            <p class="meta d-flex justify-content-center justify-content-md-start">
              <span class="calender"><i class="fa fa-calendar mr-2"><span class="visually-hidden">Date</span></i><%- posts[i].date %></span>
              <span class="comment"><i class="fa fa-comment mr-2"></i><span class="visually-hidden">Comment</span><%- posts[i].comments.length + " comments" %></span>
            </p>
            <p class="article-highlight text-center text-md-left">
              <%- posts[i].body.slice(0,90) + "....." %>
            </p>
            <div class="d-flex justify-content-center justify-content-md-start">
              <a href=<%- "/" + posts[i].slug %> class="read-more">Read More <span class="fa fa-angle-right"></span></a>
            </div>
          </div>
        </div>
        <% }} %>
      </div>
    </div>
  </main>
  <script src="https://kit.fontawesome.com/6518e17067.js" crossorigin="anonymous"></script>
</body>

</html>